apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudflared
spec:
  template:
    spec:
      volumes:
      - name: creds
        secret:
          secretName: akeyless-cf-Kubeshark
          items:
            - key: cf-Kubeshark
              path: credentials.json